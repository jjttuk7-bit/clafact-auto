CLAFACT-AUTO 8번 직접값 381건 — 1단계 2번 숫자 역할 분류 결과

1. 무엇을 했는가
- 직접값 Claim 381건의 원문에서 앞 단계가 찾은 숫자 1,456개를 모두 분류했다.
- 분류: 대상값, 증감값, 비교값, 환산값, 기간, 기준연도, 기준값, 연령, 순위, 맥락값, 제외.
- 기간·연령·순위·환산값 등은 기사 검증 대상값으로 자동 선택하지 못하게 막았다.

2. 실행 중 발견하고 해결한 공통 문제
- `263만t`, `4곳`은 숫자 추출에는 성공했지만 역할 분류에서 단위를 이어받지 못했다.
- 앞 숫자에서 멀리 떨어진 `감소` 표현을 그 숫자의 증감값으로 잘못 붙였다.
- 해결: t·곳 단위를 공통 단위 규칙에 추가하고, 중간에 다른 숫자나 과거 기록 문구가 있으면 뒤의 증감 표현을 앞 숫자와 연결하지 않도록 했다.
- 같은 규칙을 사용하는 모든 Claim에 적용되며, 특정 Claim 번호를 예외 처리하지 않았다.

3. 최종 결과
- 입력 Claim: 381건
- 분류된 숫자: 1,456개
- 역할 또는 사유 누락: 0개
- 기간·연령·순위·환산값 등 보호 역할이 자동 대상값으로 선택된 충돌: 0개
- 원문의 검증 대상 숫자를 한 개로 연결: 365건(95.8%)
- 저장 Claim 값이 기간·연령·순위·환산값이라 자동 연결을 차단: 7건
- 저장 Claim 값이 원문 검증값과 맞지 않거나 값 자체가 없음: 6건
- 같은 값이 원문에 반복돼 하나로 확정할 수 없음: 3건

4. 남은 16건의 의미
- 7건: `20대`, `상위 7개`, 원화 환산값처럼 검증 대상이 아닌 숫자가 저장 Claim 값으로 들어간 경우다.
- 6건: `5개월`을 `5개`로 저장했거나 기사값 자체가 없는 등 원래 Claim 구조화가 잘못된 경우다.
- 3건: 같은 수치가 원문에 두 번 있어 지표·문맥 확인 없이 하나를 고르면 위험한 경우다.
- 이 16건은 실패를 숨기거나 임의 선택하지 않고 다음 단계의 재구조화 대상으로 정확히 남겼다.

5. 이 단계에서 가능한 것과 아직 아닌 것
- 가능: 원문 안의 어느 숫자가 직접 검증할 대상인지, 어느 숫자를 제외해야 하는지 381건 전체에 동일 규칙으로 기록.
- 아직 아님: KOSIS 표 검색·공식값 조회·최종 일치/불일치 판정. 이번 완료 수치 365건은 KOSIS 판정 성공 수치가 아니다.
- 다음 작업: 365건의 대상 숫자를 12개 정보의 기사값·단위·기준시점과 연결하고, 남은 16건은 재구조화 사유에 따라 보정한다.

6. 근거 파일
- 결과 CSV: CLAFACT_AUTO_8번_1단계_숫자역할분류_20260825.csv
- 실행 이력 CSV: CLAFACT_AUTO_8번_1단계_숫자역할분류_실행이력_20260825.csv
- 상세 JSONL 및 검증 JSON: artifacts/direct_value_381_numeric_roles_20260825
